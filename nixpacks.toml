[phases.setup]
nixpkgs = ["python311"]

[phases.install]
commands = [
  "pip install fastapi uvicorn pillow python-dotenv requests python-multipart"
]

[start]
cmd = "uvicorn main:app --host 0.0.0.0 --port $PORT"
